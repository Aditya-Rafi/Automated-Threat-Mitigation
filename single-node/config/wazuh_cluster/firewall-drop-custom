#!/bin/sh
set -eu

# Ensure iptables exists inside wazuh.manager container.
if ! command -v iptables >/dev/null 2>&1; then
  dnf -y -q install iptables >/dev/null 2>&1 || dnf -y -q install iptables-nft >/dev/null 2>&1 || true
fi

exec /var/ossec/active-response/bin/firewall-drop "$@"
